# Session Handoff (작성: 2026-09-22)

## 목표와 완료 조건
- 목표: BATON의 원래 철학("필요 없는 컨텍스트를 처음부터 만들지 않는다")에 다가가는 방법을
  **여러 논리를 리서치·시험하는 루프**로 찾는다. 서브에이전트 위주. 본선은 요약만 읽는다.
- 완료 조건: 후보 A~E 각각에 대해 `[MEASURED]` 숫자와 실행 명령이 있는 1쪽 결과 + 판정
  (계속 / 중단 / 진실 신호 필요). 계속 판정이 하나라도 나오면 그것을 다음 구현 대상으로 확정.

## 확인된 사실과 결정
- V0 결과(2026-09-21, 코덱스 독립 재현): 사용자 실제 프롬프트 276건에 일반 규칙 v0.1.0을 돌리면
  MATCHED 25% · AMBIGUOUS 11% · NO_MATCH 65%. 개념 지도 어휘 적중 3.3%. "설계 전제 검증: 아니오".
- NO_MATCH 179건 분해(단순 표지어): 이어가기 25 · 확인 질문 8 · 짧은 반응 35 · 평소 말투 111.
- 다섯 한계: ① 값싼 층이 못 거름 ② 문장만으로 작업 못 앎(맥락) ③ 개념 지도 말 ≠ 사용자 말
  ④ 맞았는지 알 방법 없음 ⑤ 대화형 Claude에 끼어들 자리 없음.
- **소유자 결정**: 라벨링 안 함(다중 사용자 제품이라 한 사람 라벨은 무의미). 사전은 설치 사용자의
  과거 프롬프트에서 뽑고 쓰면서 갱신. 피드백 신호는 미정(사전 생성부터).
- 개인 사전 초안 있음: 작성해줘 18 · 열어줄래 13 · 진행해줘 11 · "핸드오프 읽고 이어서" 상위.
  일반 사전이 프롬프트의 23%만 읽은 이유가 목록으로 보임.

## 관련 파일·심볼
- `docs/V0_SPEC.md`: 5판 + V0 결과 절. 알려진 결함, 번복 조건.
- `docs/reviews/2026-09-21-codex-final.md`: 코덱스 최종 검토 전문.
- `observer/baton_init.py`: extract·sample·label. `baton_rules.py`: rules·score. `baton_dict.py`: 개인 사전.
- `observer/measure/`: 고정 측정 스크립트 둘.
- 데이터: `경력 기술서 26.05/.baton/{tasks.jsonl,rules.json,dict.json,calibration.md}` (gitignore).
  세션 원본 `~/.claude/projects/*26-05/*.jsonl` (24파일, 08-22~09-21), `~/.codex/sessions/` (포크 97·루트 30).

## 이미 한 변경
- 위 파일 전부 커밋·푸시됨 (sulabo/baton). 이번 세션에 `docs/research/` 신설.

## 실패한 접근법 (다시 시도 금지)
- **일반 명령형 동사 사전으로 작업 종류 분류**: 25%. 사용자 말투를 못 따라감. 확장해도 단어만 알지 뜻은 모름.
- **개념 지도 제목을 통째로 라우팅 어휘로**: 3.3%. "카테고리 트리의 정체"는 "카테고리 트리"에 안 걸림.
- **정확도 없이 MATCHED%를 성능처럼 말하기**: 코덱스가 두 번 지적. "읽음"과 "맞게 읽음"은 다르다.
- **재지 않고 숫자 옮겨 적기**: 세 번(14/91, 330→86, 19%). 전부 "수치 옆에 실행 명령" 규칙을 어긴 자리.
- **확인 없이 "고쳤다" 커밋**: 이미지 템플릿 정규식이 콜론 때문에 안 먹었는데 올림. 재실행으로 잡음.
- **설계서 안 읽고 만들기**: 첫 플러그인이 그랬음.

## 검증 증거
- 명령/확인: `python3 observer/baton_rules.py score <root>` → 68/29/179. `python3 observer/measure/codex_join_census.py` → M1=26 Mmulti=0.
  `python3 observer/baton_dict.py build <root>` → 하네스 템플릿 56행 제거 후 두 단어 구가 사용자 말로 바뀜.
- 결과: 위 수치는 코덱스가 커밋 9372d5b에서 독립 재실행해 일치.

## 남은 리스크와 다음 행동
- 리스크/블로커: 모든 후보가 "맞았는가"를 알 진실 신호가 없다. 라벨은 기각됨. 최소 신호로
  **불일치 표집**(두 방법이 갈린 것만 사람이 봄)이나 **결과 기반**(D)을 검토. 이걸 못 정하면
  라운드 결과가 또 "읽은 척" 비율이 된다.
- 다음 행동 — 리서치·시험 루프:
  ```
  후보  A 작은 모델 호출로 분류   B 세션 상태로 라우팅(프롬프트+직전 N+인계서 목표)
        C 사용 기록으로 별칭 다리   D 결과 기반 피드백(예열 밖 파일 읽음=틀림)
        E 세션 시작 상태 카드만 주입(프롬프트별 라우팅 없음)
  1라운드: B와 E (싸고 큼). 결과 → docs/research/round1-B.md, round1-E.md
  각 라운드 산출: [MEASURED] 숫자 + 실행 명령 + 판정(계속/중단/진실 신호 필요) 1쪽
  가드: 세션당 3라운드 · 숫자 못 내면 그 후보 중단 · 같은 실패 2회면 루프 중단
        서브에이전트는 읽기·결과 파일만, 커밋 금지 · 본선은 요약만
  ```
- 다음 세션 첫 행동: `docs/research/round1-*.md` 읽고 판정 → 2라운드 후보 고르기.

## 승격 후보 (DECISIONS.md로 올릴 것)
- "라벨링은 하지 않는다. 사전은 사용자별 과거 프롬프트에서 뽑는다" — 소유자 결정(2026-09-22).
  신호: 방향을 정한 결정. 버린 대안(일반 규칙+라벨 검증)과 이유(다중 사용자 제품)는 소유자 몫.
- "V0 판정 아니오, 멈춤 후 연구 루프로 전환" — 2026-09-21~22. 버린 대안: 사전 확장 즉시 착수, Jev 즉시 도입.

### 기각됨 (다시 올리지 않는다)
- 없음
