# Session Handoff (작성: 2026-09-22)

## 목표와 완료 조건
- 목표: BATON 원래 철학("필요 없는 컨텍스트를 처음부터 만들지 않는다")에 다가가는 방법을
  후보 A~E를 **리서치·시험하는 루프**로 찾는다. 서브에이전트 위주, 본선은 요약만.
- 완료 조건: 후보마다 `[MEASURED]` 숫자 + 실행 명령 + 판정(계속/중단/진실 신호 필요) 1쪽.
  "계속"이 하나라도 나오면 그것이 다음 구현 대상. 전부 "중단"이면 진실 신호부터 정한다.

## 확인된 사실과 결정
- V0(09-21, 코덱스 독립 재현): 실제 프롬프트 276건에 일반 규칙 → MATCHED 25% · AMBIGUOUS 11% · NO_MATCH 65%.
  개념 지도 어휘 적중 3.3%. 판정 "설계 전제 검증: 아니오".
- 다섯 한계: ① 값싼 층이 못 거름 ② 문장만으로 작업 못 앎 ③ 개념 말 ≠ 사용자 말 ④ 맞았는지 알 방법 없음 ⑤ 대화형 Claude에 끼어들 자리 없음.
- **소유자 결정(09-22)**: 라벨링 안 함(다중 사용자 제품). 사전은 사용자별 과거 프롬프트에서. 피드백 신호 미정.
- **1라운드 B(세션 상태로 라우팅) 판정: 중단.** 맥락을 붙이면 NO_MATCH는 줄지만 MATCHED로 안 가고 AMBIGUOUS로 간다.

  | 조건 | MATCHED | AMBIGUOUS | NO_MATCH |
  |---|---|---|---|
  | 단독 | 68 | 29 | 179 |
  | 직전 2개 붙임 | 85 | 89 | 102 |
  | 직전 3개 붙임 | 75 | 114 | 87 |

  N=3에서 NO_MATCH 92 감소 = MATCHED +7, AMBIGUOUS +85. 단독 MATCHED 27건이 맥락 때문에 AMBIGUOUS로 깨짐.
  표는 내가 직접 재실행해 **정확히 일치**. 단 B의 "44/44가 앞 프롬프트 라벨 상속"은 내 정의로 21/44라 **미확정**, B에 정의 확인 요청함.
- **1라운드 E(세션 시작 상태 카드) 실행 중.** 에이전트 `round1-E`. 결과 예정 `docs/research/round1-E.md`.
- 진실 신호 없이는 라우팅 개선을 우연 적중과 못 가른다 — 이 세션에서 코덱스 2회, 나, B까지 **네 번째 같은 결론**.

## 관련 파일·심볼
- `docs/V0_SPEC.md` 5판+결과 절 · `docs/reviews/2026-09-21-codex-final.md` · `docs/research/round1-B.md`(미커밋, 절대경로 4줄 치환 대기)
- `observer/baton_init.py`(extract·sample·label) · `baton_rules.py`(rules·score, `classify_task_type`) · `baton_dict.py`(개인 사전)
- `observer/research/round1_b.py`(미커밋) · `observer/measure/`(고정 측정 둘)
- 데이터(gitignore): `경력 기술서 26.05/.baton/{tasks.jsonl,rules.json,dict.json,calibration.md}` · 원본 `~/.claude/projects/*26-05/*.jsonl` 24파일

## 이미 한 변경
- 커밋·푸시됨: V0 결과 절, 개인 사전(`baton_dict.py`, 하네스 이미지 템플릿 56행 제거), 이 인계서.
- **미커밋**: `docs/research/round1-B.md`, `observer/research/round1_b.py` (B가 경로 치환 중. 치환 확인 후 커밋).
- 경력기술서 저장소 `.gitignore`에 `.baton/` 추가함(09-21).

## 실패한 접근법 (다시 시도 금지)
- **일반 명령형 동사 사전으로 분류**: 25%. 사용자 말투 밖.
- **개념 지도 제목을 통째로 어휘로**: 3.3%.
- **맥락 창 키우기(B)**: NO_MATCH→AMBIGUOUS로 갈 뿐 MATCHED 안 늚. 잘 읽던 것도 깨짐. 창을 더 키우는 변형 금지.
- **MATCHED%를 정확도처럼 말하기**: 코덱스 2회 지적.
- **재지 않고 숫자 옮겨 적기**: 세 번(14/91, 330→86, 19%). **서브에이전트 세부 주장 대조 없이 옮기기**: 44/44.
- **확인 없이 "고쳤다" 커밋**: 이미지 템플릿 정규식(콜론). 재실행이 잡음.

## 검증 증거
- 명령: `cd observer/research && python3 round1_b.py` → 68/29/179, N=2 85/89/102, N=3 75/114/87. 내 재실행 일치.
- 명령: 직전 1개 단독 판정 == N=2 판정 종류 → 21/44 (B 주장 44/44 미재현).
- 명령: `~/.claude/skills/handoff/promote.sh <baton>` → 검토 1일 1판, 0/0/0. 셀 이력이 없는 것이지 "없음"이 아님.

## 남은 리스크와 다음 행동
- 리스크/블로커: **진실 신호 부재.** 라벨 기각됨. 최소 후보 둘 — 불일치 표집(두 방법이 갈린 것만 봄) / 결과 기반(D: 예열 밖 파일 읽음=틀림).
  이걸 안 정하면 A·C도 "읽은 척" 비율만 나온다. E는 분류를 안 하므로 이 벽을 피할 수 있는 유일한 후보.
- 다음 행동(순서대로):
  1. `docs/research/round1-E.md` 읽기. E 판정이 "계속"이면 2라운드 = E 심화(실제 주입 A/B 설계).
  2. B의 44/44 정의 답 확인 → 파일에 정의 명시 → 절대경로 치환 확인 → B·E 결과와 스크립트 커밋.
  3. E도 "중단"이면 라운드 진행 전에 **진실 신호를 소유자와 정한다.** 정하기 전엔 A·C 안 띄움.
  4. 인계서 갱신. 가드: 세션당 3라운드 · 같은 실패 2회 중단 · 서브에이전트 커밋 금지.

## 승격 후보 (DECISIONS.md로 올릴 것)
- "라벨링은 하지 않는다. 사전은 사용자별 과거 프롬프트에서 뽑고 쓰면서 갱신한다" / 소유자 결정 09-22 / 버린 대안·이유는 소유자 몫.
- "V0 판정 아니오. 멈춤 후 연구 루프로 전환" / 09-21~22 / 버린 대안: 사전 확장 즉시, Jev 즉시.
- **"진실 신호를 정하지 않고는 라우팅 후보를 더 시험하지 않는다"** / 같은 결론이 독립 검토자 넷(코덱스×2·나·B)에서 하루 안에 반복 / 3일 규칙 밖이라 자동 후보는 아니고 판단으로 올림.

### 기각됨 (다시 올리지 않는다)
- 없음
