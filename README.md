# Handoff — 세션 인계서를 쓰면서 영구 기록으로 올릴 것을 함께 골라내는 스킬

Claude Code 스킬입니다. 긴 대화를 억지로 끌고 가는 대신 작업 상태를 짧은 파일로 남기고,
**그 김에 "이건 매번 다시 정하고 있는 것 아닌가"를 판정합니다.**

## 두 가지 일을 합니다

### 1. 인계서 쓰기

저장소 루트에 `HANDOFF.md`를 씁니다. 여덟 항목을 채우고, 매번 덮어씁니다.

값이 제일 나가는 항목은 **"실패한 접근법"**입니다. 다음 세션이 같은 삽질을
처음부터 반복하지 않게 막아줍니다.

### 2. 승격 검토

인계서는 덮어쓰니까 개별 판은 휘발성입니다. 그런데 **판을 가로질러 반복되는 것은
휘발성이 아닙니다.** 같은 판단을 세 번 다시 했다면 그건 세션 상태가 아니라 상시 규칙이고,
매번 다시 판단하는 비용을 내고 있다는 뜻입니다.

과거 판을 따로 보관하지 않습니다. **git 이력이 곧 보관소입니다.**

## 신호는 셋, 성격이 다릅니다

| 신호 | 무엇을 보나 | 자동 탐지 |
|---|---|---|
| 반복 실패 | 같은 "실패한 접근법"이 **서로 다른 3일 이상** 살아남음 | 된다 |
| 탈락 | 앞선 판에 있던 상시 제약이 최신판에서 조용히 빠짐 | 된다 |
| 번복 | 결정이 반대로 뒤집힘 | **안 된다** |

번복은 자동 탐지를 시도했다가 **뺐습니다.** 의미 대조라 문자열 집계로는 안 잡힙니다.
못 하는 걸 한다고 써 두면 못 잡은 것을 "없음"으로 보고하게 됩니다.

## 판이 아니라 날로 셉니다 — 이게 신호의 거의 전부를 가릅니다

인계서는 하루에 여러 번 커밋되고, 그때 "실패한 접근법" 절이 통째로 복붙됩니다.
실측에서 어떤 저장소의 최근 8판 중 **3판이 이전 판과 완전히 동일**했습니다.

커밋 단위로 세면 그 복붙이 그대로 "반복"으로 잡힙니다.

| 세는 단위 | 3회 이상 살아남은 항목 |
|---|---|
| 커밋 8판 | 8건 |
| **서로 다른 8일** | **1건** |

**8건 중 7건이 같은 날 여러 번 커밋한 부산물이었습니다.** 남은 1건은 진짜 상시 제약이었고요.

서로 다른 날에 살아남았다는 것은 여러 작업 세션을 건너뛰고도 남았다는 뜻이고,
그게 상시 규칙의 증거입니다.

## 탈락이 예상보다 값집니다

설계할 때는 곁가지로 넣었는데 실측에서 제일 쓸모 있었습니다.

어떤 저장소의 인계서에서 이런 것들이 빠져 있었습니다.

```
공개 범위를 바꾼 소유자 결정
마스킹 규칙
(번복 기록 자체도 여기 섞여 있었다)
```

**소유자 결정이 덮어쓰기 파일에만 살다가 사라진 겁니다.** 번복을 자동으로 잡으려던 시도가
오히려 번복 기록이 새는 걸 놓칠 뻔했습니다.

## 설치

```bash
git clone https://github.com/sulabo/handoff-skill.git ~/.claude/skills/handoff
chmod +x ~/.claude/skills/handoff/*.sh
```

Claude Code에서 "핸드오프 작성해줘", "인계서 남겨줘", "승격 검토"라고 하면 발동합니다.

`HANDOFF.md`가 **git에 추적되고 있어야** 승격 검토가 동작합니다. 과거 판을 못 보면
이 검토는 성립하지 않습니다.

```bash
./promote.sh <저장소 경로> [판수(기본 8)]
```

## 중요한 선 하나

**이 스킬은 `DECISIONS.md`를 혼자 판단해서 쓰지 않습니다.**

결정 기록의 값은 "무엇을 정했나"가 아니라 **"왜, 무엇을 버렸나"**에 있습니다.
버린 대안과 그 이유는 결정한 사람 머릿속에만 있고, 인계서에서 역산할 수 없습니다.
추측으로 채우면 6개월 뒤에 읽을 때 가장 중요한 줄이 거짓이 됩니다.

그래서 후보와 초안까지만 만들고, 버린 대안과 이유는 빈칸으로 둡니다.

## 0건은 "없음"이 아닐 수 있습니다

저장소마다 인계서 절 제목이 다릅니다. 어떤 판은 `## E. 실패한 접근법`이고,
어떤 판은 그 절이 아예 없습니다.

처음에는 `## 실패한 접근법`으로 고정해 찾다가 **어떤 저장소에서 0건**이 나왔습니다.
오류는 없고 결과만 0이었으니, 그대로 "승격 후보 없음"으로 보고됐을 겁니다.

지금은 추출이 빈 판을 "형식 불일치"로 따로 세서 분모에서 빼고, 그 사실을 출력합니다.

## HANDOFF와 DECISIONS의 경계

| | HANDOFF.md | DECISIONS.md |
|---|---|---|
| 수명 | 이번 작업이 끝나면 쓸모없어진다 | 6개월 뒤에도 "왜?"의 답 |
| 판수 | 최신 1부, 덮어쓴다 | 누적, 지우지 않는다 |
| 쓰는 이 | 에이전트가 쓴다 | **소유자가 쓴다** |

애매하면 기준은 하나입니다. **이 문장이 다음 주에도 참인가.**

## 한계 (정직하게)

- 번복 자동 탐지는 못 합니다. 소유자가 알려줄 때만 후보로 올립니다.
- 탈락 신호는 인계서 절 제목이 판마다 같아야 동작합니다.
- 문턱 3일은 저장소 한 곳의 분포로 정했습니다. 다른 곳에서도 같은 지점에서
  갈리는지는 아직 모릅니다.
- git 미추적 저장소에서는 승격 검토가 아예 동작하지 않습니다.

## 짝이 되는 스킬

[ontology](https://github.com/sulabo/ontology-skill) — 이 스킬이 올린 승격 후보가
`DECISIONS.md`에 결정으로 들어가면, 그 결정이 건드린 개념을 온톨로지가 역산해
재확인 대상으로 잡습니다. 코드는 그대로인데 결정만 움직인 상태가 곧 미구현입니다.

## 라이선스

MIT
